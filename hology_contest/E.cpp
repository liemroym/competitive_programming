#include <bits/stdc++.h>

using namespace std;

int main() {
    vector<vector<int>> a;
    int n, m;
    cin >> n >> m;
    for (int i = 0; i < n; i++) {
        vector<int> temp;
        for (int j = 0; j < m; j++) {
            int inp;
            cin >> inp;
            temp.push_back(inp);
        }
        a.push_back(temp);
    }

    int j = 9;
    while (j > 0) {
        for (int y = 0; y < n; y++) {
            for (int x = 0; x < m; x++) {
                        
            }
        }
    }
    return 0;
}
