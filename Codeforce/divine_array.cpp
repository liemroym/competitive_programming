#include <bits/stdc++.h>
using namespace std;

int main() {
    int t;
    cin >> t;
    for (int i = 0; i < t; i++) {
        vector<int> a;
        int n;
        cin >> n;
        for (int j = 0; j < n; j++) {
            int temp;
            cin >> temp;
            a.push_back(temp);
        }
        int q;
        cin >> q;
        for (int j = 0; j < q; j++) {
            int x, k;
        }
    }
    return 0;
}
